<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-dark bg-dark">
        <div class="container">
            <span class="navbar-brand mb-0 h1">OAuth App</span>
            <a href="/logout" class="btn btn-outline-light">Logout</a>
        </div>
    </nav>

    <div class="container mt-5 text-center">
        <div class="card p-5 shadow-sm">
            <img src="<%= user.image %>" alt="Profile" class="rounded-circle mx-auto d-block mb-3" width="100">
            <h1 class="display-4">Welcome, <%= user.displayName %>!</h1>
            <p class="lead">You are successfully logged in.</p>
            <hr>
            <p>Your Email: <strong><%= user.email %></strong></p>
        </div>
    </div>
</body>
</html>